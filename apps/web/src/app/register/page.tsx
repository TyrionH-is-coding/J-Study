import { FeaturePlaceholder } from "@/components/shell/feature-placeholder";

export default function RegisterPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Account"
      title="Register"
      description="Registration and invite behavior are outside the foundation rebuild."
    />
  );
}
