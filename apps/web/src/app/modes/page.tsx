import { FeaturePlaceholder } from "@/components/shell/feature-placeholder";

export default function ModesPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Workspace"
      title="Learning modes"
      description="Mode discovery will remain multi-discipline when the product workflow is connected."
    />
  );
}
